{
  "name": "gdoc",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc -p tsconfig.json --noEmit && tsc -p viewer/tsconfig.json --noEmit && vitest run",
    "gdoc": "bun run cli/index.ts"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@supabase/supabase-js": "^2.108.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "trustedDependencies": [
    "onnxruntime-node",
    "protobufjs"
  ]
}
